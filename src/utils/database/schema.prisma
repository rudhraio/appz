generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DBURL")
}

model UserModel {
  aid      Int     @id @default(autoincrement())
  email    String  @unique
  username String?
  fname    String?

  @@map("users")
}
